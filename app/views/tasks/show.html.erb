<h1><%= @task.title %></h1>
<p class = "detail"><%= @task.details %></p> 
<input type="checkbox" <%= 'checked' if @task.completed %> >
<span class = "quote"> <%= @task.completed ? "This task is completed" : "This task is not completed yet" %> </span> 
<p> <%= link_to "Back to list", tasks_path %> </p>
